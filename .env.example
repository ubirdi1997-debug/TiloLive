# Admin Panel Authentication
ADMIN_PASSWORD=admin123

# SMTP Settings (optional - can also be configured in data/db.json)
# SMTP_HOST=smtp.hostinger.com
# SMTP_PORT=587
# SMTP_USER=your-email@tilolive.in
# SMTP_PASS=your-password
